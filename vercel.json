{
	"builds": [
		{
			"src":"./asgi.py",
			"use":"@vercel/python",
			"config": {
        				"installCommand": "pip install -r requirements.txt --no-deps"
      				  }
		}
	],
	"routes": [
		{
			"src":"/(.*)",
			"dest":"/"
		}
	]
}
